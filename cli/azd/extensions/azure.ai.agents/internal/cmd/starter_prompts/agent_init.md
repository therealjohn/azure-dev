Initialize a Microsoft Foundry agent in this project at {{.ProjectPath}}.

This directory contains the AZD AI bootstrap stub written by `azd ai agent
init`. Use the AZD AI skill{{if .SkillPath}} (installed at {{.SkillPath}}){{end}} to drive the end-to-end flow:
scaffold, provision, deploy, verify. Pull `azd ai doc agent <topic>` (samples,
initialize, develop, configure, extend, deploy, evaluate, operate, investigate)
for the details of any step BEFORE running write commands. Confirm with me
before picking a Foundry project or model, and before any step that would
create, change, or bill against cloud resources.
